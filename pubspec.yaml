name: clean_feature_arch
description: The Absolute Rule Architecture - Scaffolder and Enforcer
version: 1.0.0
publish_to: none # Remove when ready to publish to pub.dev

environment:
  sdk: ^3.3.0

dependencies:
  args: ^2.5.0
  recase: ^4.1.0
  path: ^1.9.0
  mason_logger: ^0.3.1
  analyzer: ^6.4.1
  custom_lint_builder: ^0.6.2
  yaml: ^3.1.2

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  clean_feature_arch: make
