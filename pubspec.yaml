name: clean_feature_arch
description: The Absolute Rule Architecture - Scaffolder and Enforcer
version: 1.0.0
repository: https://github.com/landxcape/clean_feature_arch
homepage: https://github.com/landxcape/clean_feature_arch

environment:
  sdk: ^3.3.0

dependencies:
  args: ^2.7.0
  recase: ^4.1.0
  path: ^1.9.1
  mason_logger: ^0.3.5
  analyzer: ^12.1.0
  analysis_server_plugin: ^0.3.14
  analyzer_plugin: ^0.14.8
  yaml: ^3.1.3

dependency_overrides:
  meta: 1.17.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0

executables:
  clean_feature_arch: make
